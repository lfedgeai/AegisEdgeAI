# Unified-Identity - Phase 1: SPIRE API & Policy Staging (Stubbed Keylime)
# SPIRE Agent configuration for Python app demo

agent {
    # SPIRE requires availability_target to be at least 24 hours (demo default: 24h)
    availability_target = "24h0m0s"
    data_dir = "/opt/spire/data/agent"
    log_level = "DEBUG"
    server_address = "localhost"
    server_port = 8081
    trust_bundle_path = "/tmp/bundle.pem"
    trust_domain = "example.org"
    socket_path = "/tmp/spire-agent/public/api.sock"
    
    # Unified-Identity - Phase 1: Enable feature flag
    experimental {
        feature_flags = ["Unified-Identity"]
    }
}

plugins {
    NodeAttestor "join_token" {
        plugin_data {
        }
    }

    KeyManager "disk" {
        plugin_data {
            directory = "/opt/spire/data/agent"
        }
    }

    WorkloadAttestor "unix" {
        plugin_data {
            discover_workload_path = true
        }
    }
}

