# Unified-Identity - Phase 1: SPIRE API & Policy Staging (Stubbed Keylime)
# SPIRE Agent configuration for Kubernetes integration
# Running outside Kubernetes, accessible to CSI driver

agent {
    data_dir = "/opt/spire/data/agent"
    log_level = "DEBUG"
    server_address = "localhost"
    server_port = 8081
    trust_bundle_path = "/opt/spire/data/agent/bundle.crt"
    trust_domain = "example.org"
    
    # Unified-Identity - Phase 1: Enable feature flag
    feature_flags {
        Unified-Identity = true
    }
    
    # Workload API socket - accessible to CSI driver
    socket_path = "/tmp/spire-agent/public/api.sock"
}

plugins {
    NodeAttestor "join_token" {
        plugin_data {
        }
    }

    KeyManager "memory" {
        plugin_data {
        }
    }

    WorkloadAttestor "k8s" {
        plugin_data {
            # For external agent, we may need to configure kubeconfig path
            # kubeconfig_file_path = "/opt/spire/kubeconfig"
            skip_kubelet_verification = true
        }
    }
}

health_checks {
    listener_enabled = true
    bind_address = "0.0.0.0"
    bind_port = 8080
    live_path = "/live"
    ready_path = "/ready"
}

