#!/bin/bash
# Unified-Identity - Phase 1: SPIRE API & Policy Staging (Stubbed Keylime)
# Integration test teardown for sovereign attestation

set -e -o pipefail

. "${ROOTDIR}/common/common.sh"

log-info "Tearing down sovereign attestation integration test..."

# Clean up binaries
rm -f "${ROOTDIR}/suites/sovereign-attestation/spire-server"
rm -f "${ROOTDIR}/suites/sovereign-attestation/spire-agent"

log-info "Sovereign attestation test teardown complete"

