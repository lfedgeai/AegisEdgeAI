# Unified-Identity - Phase 2: Core Keylime Functionality (Fact-Provider Logic)
# Example Keylime Verifier configuration with Unified-Identity feature enabled
#
# Copy this to /etc/keylime/verifier.conf and modify as needed

[verifier]
# Enable Unified-Identity Phase 2 feature
# Set to true to enable tpm-app-key verification flow
unified_identity_enabled = true

# Default facts (used when facts not found in database/store)
# These are fallback values for Phase 2 testing
unified_identity_default_geolocation = Spain: N40.4168, W3.7038
unified_identity_default_integrity = passed_all_checks
unified_identity_default_gpu_status = healthy
unified_identity_default_gpu_utilization = 15.0
unified_identity_default_gpu_memory = 10240

# Standard Keylime Verifier configuration
# (add your existing verifier configuration here)

